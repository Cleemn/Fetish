<div class="navbar_top">
  <% if user_signed_in? %>
  <div class="connected connection d-flex align-items-center justify-content-center p-2">
    <%= link_to root_path do %><%= image_tag "logo.png", width: 60, height: 60, crop: :fill %>
    <% end %>
  </div>
  <% else %>
  <div class="connection d-flex align-items-center justify-content-between p-2">
    <%= image_tag "logo.png", width: 60, height: 60, crop: :fill %>
    <%= link_to new_user_session_path do %><i class="fas fa-user fa-2x" style="color:#FFFFFF"></i><% end %>
  </div>
  <% end %>
</div>

